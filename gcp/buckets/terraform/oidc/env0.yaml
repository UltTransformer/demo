version: 2
deploy:
  steps:
    setupVariables:
      after:
        - name: Set OIDC Token For GCP
          run: |
            echo Pushing OICD toekn into text file
destroy:
  steps:
    setupVariables:
      after:
        - name: Set OIDC Token For GCP
          run: |
            echo Pushing OICD toekn into text file
            
            