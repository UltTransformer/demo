version: 1

deploy:
  steps:
    setupVariables:
      after:
        - aws cloudformation deploy --stack-name cf-vpc --template-file vpc.yaml --region eu-north-1
destroy:
  steps:
    setupVariables:
      after:
        - aws cloudformation delete-stack --stack-name cf-vpc --region eu-north-1