version: 2
deploy:
  steps:
    terraformInit:
      before:
        - name: Create String
          run: |
            echo "$ENV0_ENVIRONMENT_ID"
            env_region=$( env0.auto.tfvars.json | jq '.region' )
            print ${env_region}

