version: 2
deploy:
  steps:
    terraformInit:
      before:
        - name: Create String
          run: |
            echo "$ENV0_ENVIRONMENT_ID"
            env_region=$( cat env0.auto.tfvars.json | jq -r '.region' )
            echo ${env_region}
            echo "$ENV0_ENVIRONMENT_ID,.,${env_region}"

