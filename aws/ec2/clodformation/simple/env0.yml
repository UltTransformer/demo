# version: 1

deploy:
  steps:
    setupVariables:
      after:
        - aws configure set default.region eu-north-1
destroy:
  steps:
    setupVariables:
      after:
        - aws configure set default.region eu-north-1
